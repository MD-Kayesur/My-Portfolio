 

 

const Header = ({title}) => {
    return (
        <div>
            <h1 className="text-4xl font-bold bg-[#1a1d52] text-white text-center">{title}</h1>
             
        </div>
    );
};

export default Header;